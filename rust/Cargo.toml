[package]
name = "chromatic-search"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
nauty-Traces-sys = { version = "0.11", features = ["native"] }

[profile.release]
opt-level = 3
lto = "thin"
